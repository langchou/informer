logconfig:
  file: "data/app.log"
  maxSize: 10
  maxBackups: 5
  maxAge: 30
  compress: true
  level: "info"

dingtalk:
  token: ""
  secret: ""

proxyPoolAPI: ""

# Chiphell配置
cookies: ""
userKeyWords:
  "158********":
    - "iphone"

waitTimeRange:
  min: 2
  max: 5
